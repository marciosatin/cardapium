{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Cardápio gerado em {{ "now"|date("d/m/Y") }}
                    </h3>
                </div>
                <div class="panel-body">
                    <ul>
                        {% for meals in menu %}
                        <li>
                            <span>{{ meals['almoco']['meal']['meal_name'] }}</span>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

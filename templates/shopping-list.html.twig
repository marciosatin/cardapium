{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            {% include 'display-erros.html.twig' %}
            <div class="panel col-xs-12">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Lista de compras
                    </h3>
                </div>
                <div class="panel-body">
                    <form class="form-inline text-center" method="get" action="{{ route('shopping-list.list') }}">
                        <div class="form-group">
                            <label class="control-label">Início</label>
                            <input class="form-control" type="date" placeholder="DD/MM/YYY"
                                   name="dtInicio" value="{{ dtInicio }}">
                        </div>
                        <div class="form-group">
                            <label class="control-label">Fim</label>
                            <input class="form-control" type="date" placeholder="DD/MM/YYY"
                                   name="dtFim" value="{{ dtFim }}">
                        </div>
                        <div class="form-group">
                            <select class="form-control"
                                    name="menu_id">
                                {%  for menu in menus %}
                                    <option {{ (menu.id == menu_id) ? "selected": "" }} value="{{ menu.id }}">
                                        {{ menu.name }}
                                    </option>
                                {% endfor %}            
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary notika-btn-primary">
                            <span class="glyphicon glyphicon-search"></span>
                        </button>
                    </form>
                    <div class="col-xs-12">
                        <table class="table">
                            <thead>
                                <tr>
{#                                    <th>#</th>#}
                                    <th>Nome</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for item in itens %}
                                    <tr>
{#                                        <td>{{ item.id }}</td>#}
                                        <td>{{ item.name }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block scripts %}
    <link rel="stylesheet" href="/css/shopping-list-print.css">
{% endblock %}
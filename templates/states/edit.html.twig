{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Editar estado
                    </h3>
                </div>
                <div class="panel-body">
                    {% set action = route('states.update', {'id': state.id}) %}
                    {% set submitIcon = 'floppy-disk' %}
                    {% include 'states/_form.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
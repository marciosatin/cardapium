{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            {% include 'display-erros.html.twig' %}
            <div class="panel col-xs-12">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Editar ingrediente
                    </h3>
                </div>
                <div class="panel-body">
                    {% set action = route('ingredients.update', {'id': ingredient.id}) %}
                    {% set submitIcon = 'pencil' %}
                    {% include 'ingredients/_form.html.twig' %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
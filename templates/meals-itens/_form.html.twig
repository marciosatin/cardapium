<form method="post" action="{{ action }}">
    <input class="form-control" type="hidden" name="meal_id" value="{{ meal.id }}">
    <div class="form-group">
        <label class="control-label">Ingrediente</label>
        <select class="form-control"
                name="ingredient_id">
            {%  for ingredient in ingredients %}
                <option value="{{ ingredient.id }}">
                    {{ ingredient.name }}
                </option>
            {% endfor %}            
        </select>
    </div>
    <div class="form-group">
        <label class="control-label">Estado</label>
        <select class="form-control" value="{{ state.id }}"
                name="state_id">
            {%  for state in states %}
                <option value="{{ state.id }}">
                    {{ state.name }}
                </option>
            {% endfor %}            
        </select>
    </div>
    <div class="form-group">
        <label class="control-label">Tipo</label>
        <select class="form-control"
                name="type_id">
            {%  for type in types %}
                <option value="{{ type.id }}">
                    {{ type.name }}
                </option>
            {% endfor %}            
        </select>
    </div>
    <div class="form-group">
        <label class="control-label">Quantidade</label>
        <input class="form-control" type="text" placeholder="Quantidade"
               name="quantity">
    </div>   
    <button type="submit" class="btn btn-primary btn-block">
        Adicionar
        <span class="glyphicon glyphicon-{{ submitIcon }}"></span>
    </button>
</form>
